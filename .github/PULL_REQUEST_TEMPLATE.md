## Summary

<!-- What does this PR do and why? -->

## Checklist

- [ ] Test suite passes locally: `python3 -m unittest discover -s . -p "test*.py"`
- [ ] New/updated tests cover the behaviour change
- [ ] No new runtime dependencies added (`pulse.py` stays stdlib-only)
- [ ] README and/or `docs/ARCHITECTURE.md` updated if user-visible behaviour changed
